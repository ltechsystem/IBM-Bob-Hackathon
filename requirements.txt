# ============================================
# Sentinel + Bob Debug Agent — Python dependencies
# ============================================
# Install with:
#   pip install -r requirements.txt
#
# Python 3.10+ required
# ============================================

# IBM i connection via XMLSERVICE HTTP transport
itoolkit>=1.7.0

# Load .env file into environment variables
python-dotenv>=1.0.0

# File system watcher (used for local file watching utilities)
watchdog>=4.0.0

# Rich terminal output — coloured diffs, panels, tables, progress
rich>=13.0.0

# ============================================
# Bob Debug Agent — Web API backend
# ============================================

# HTTP API framework
fastapi>=0.111.0

# ASGI server
uvicorn[standard]>=0.29.0

# Data validation / serialisation
pydantic>=2.7.0

# watsonx.ai IBM Granite inference
# (optional — set BACKEND_STUB=true to skip; required for live AI inference)
ibm-watsonx-ai>=1.0.0

# ============================================
# Dev / test
# ============================================
# FastAPI test client (requires httpx)
httpx>=0.27.0
